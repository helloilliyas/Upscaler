# Development + CI dependencies (superset of runtime).
-r requirements.txt

pytest>=8.2
httpx>=0.27
ruff>=0.5
mypy>=1.10

# CPU-only, for exercising the diffusion tile-stitching math in tests.
numpy<2
