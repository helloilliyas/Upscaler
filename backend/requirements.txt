# Runtime dependencies for the FastAPI backend (no GPU / model libraries here;
# those live in the Modal worker images).
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
Pillow>=10.3
google-auth>=2.30
