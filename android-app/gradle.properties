org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# --- App configuration (non-secret; baked into BuildConfig at build time) ---
# The Google *Web* OAuth client id (token audience the backend verifies).
GOOGLE_WEB_CLIENT_ID=365004651996-233btljubl9a8ulqf9um2mumrr26psp5.apps.googleusercontent.com
# Modal backend endpoint (keep the trailing slash):
BASE_URL=https://helloilliyas--ai-photo-restorer-fastapi-app.modal.run/
