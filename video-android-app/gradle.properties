org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# --- App configuration (non-secret; baked into BuildConfig at build time) ---
# The Google *Web* OAuth client id (token audience the backend verifies).
# Same approved account as the photo app; the video app is a separate Android
# OAuth client (new package name, same committed debug-keystore SHA-1).
GOOGLE_WEB_CLIENT_ID=365004651996-233btljubl9a8ulqf9um2mumrr26psp5.apps.googleusercontent.com
# Video backend endpoint (keep the trailing slash):
VIDEO_BASE_URL=https://helloilliyas--ai-video-upscaler-fastapi-app.modal.run/
